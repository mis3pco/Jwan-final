# نموذج بيانات جوان

`users/{uid}`: بيانات الحساب، الدور (`customer|driver|admin`)، الهاتف، السكن، المستندات، حالة التفعيل، الرصيد، النشاط.

`users/{uid}/tokens/{tokenId}`: رموز Web Push الخاصة بالمستخدم.

`orders/{orderId}`: كل طلب مع `customerId`, `driverId`, الاستلام، الوجهة، النوع، السعر، الحالة، أوقات القبول والاستلام والوصول والتأكيد، والعمولة.

`topups/{topupId}`: طلبات شحن العميل أو السائق، المبلغ، طريقة الدفع، رابط الإشعار، الحالة والمراجعة.

`withdrawals/{withdrawalId}`: طلبات سحب السائق، المبلغ، الطريقة، الحساب، الحالة.

`transactions/{transactionId}`: سجل محاسبي لا يكتبه العميل مباشرة.

`ratings/{ratingId}`: تقييم العميل للخدمة بعد الإكمال.

`orderComments/{commentId}`: تعليقات السائقين على الطلب والسعر.

`support/{ticketId}`: مراسلات الدعم بين المستخدم والإدارة.

`orderPriceEvents/{eventId}`: أحداث تحديد سعر الطلب من الإدارة لإرسال إشعار للسائقين.

الفكرة: البيانات كلها مرتبطة بالمستخدم من خلال UID، ويمكن استخراج طلبات كل عميل أو كل سائق بواسطة `customerId` أو `driverId` دون إنشاء قاعدة منفصلة لكل شخص.
